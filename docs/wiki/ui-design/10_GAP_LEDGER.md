# 10 当前实现差距台账

> 文档编号：UI-10  
> 规范版本：1.1.0-draft  
> 状态：草案  
> 最后核对日期：2026-09-01
> 适用提交：522200aee
> 维护角色：设计系统维护者  
> 相关文档：[组件目录](components/README.md) · [维护流程](11_MAINTENANCE.md)

## 初学者解释

规范描述“应该到哪里”，台账描述“现在离那里多远”。差距不是立即改代码的授权，而是后续工作可以直接领取的任务卡。每张卡要说明证据、影响、入口和验收，避免只写“UI 不统一”。

## 规范要求

所有差异必须按本页模板记录证据、目标、风险、步骤和验收；不得用含糊描述代替任务卡，也不得把未实施目标标为已完成。

## 优先级

| 优先级 | 含义 | 示例 |
|---|---|---|
| P0 | 阻塞使用、安全、隐私或发布 | 关键按钮不可点击、敏感信息泄漏 |
| P1 | 关键功能或跨风格一致性 | 同一状态在不同风格含义不同 |
| P2 | 普通统一与维护成本 | 同义组件重复、常用 Token 缺失 |
| P3 | 可选体验优化 | 更细的动画、装饰与次要效率 |

## 任务卡字段

每张任务卡必须包含：ID、领域、状态、维护角色、优先级、现状证据、目标、Why、影响、修改入口、依赖、风险、实施步骤、自动验证和人工验证。状态只能是“待评估、待实施、进行中、已阻塞、已完成、接受例外”。

## 当前任务卡

### UI-GAP-007：无液态玻璃 Miuix 适配

| 字段 | 内容 |
|---|---|
| 领域 / 优先级 / 状态 | 全局视觉 / P2 / 进行中（代码已实现，待编译与设备验收） |
| 维护角色 | 设计系统与各页面维护者 |
| 现状证据 | `79e9003b2` 已由主入口和独立视频入口传递玻璃状态；无玻璃悬浮底栏走普通 Miuix 基元；共享 Tab 使用 42/12 或紧凑 36/10 几何并随字体增高；`522200aee` 使实色首页和设置顶栏不再创建闲置采样源 |
| 目标 | 官方组件优先，无玻璃独立几何，实色内容卡与可选普通模糊，保持双预设业务合同 |
| Why | 关闭效果后仍应有完整的层级、可读性和低成本渲染路径 |
| 影响 | 设置、导航、信息流、搜索、动态、内容库、详情、评论、音频、直播、番剧及长尾页面 |
| 修改入口 | AppThemeConfig、AppShapes、AppSurfaceTokens、AppChromeSizeTokens、App* renderer 与现有业务 policy |
| 依赖 / 风险 | 维持现有依赖；保护壁纸/皮肤、字体偏好、搜索展开、播放生命周期和玻璃分支 |
| 实施步骤 | 规范 → 状态与基础组件 → 设置/导航 → 页面族与静态回归 |
| 自动验证 | 已补配置、材质、几何、底栏策略、设置外壳及迁移边界测试；本批按指令只完成差异、分隔符、冲突标记和文档链接静态检查，未运行 Gradle |
| 人工验证 | 无玻璃四种模糊组合 × 明暗主题/动态色 × 手机/平板/分屏；字体 1.0/1.3/2.0；回归 MD3 与玻璃开启 |

| 批次 | 实施状态 | 验证状态 |
|---|---|---|
| 规范与历史描述修订 | 已完成 | 静态文档检查；不代表 UI 已适配 |
| 统一状态、材质与基础组件 | 代码已实现 | 静态检查通过；未编译、未设备验收 |
| 设置与应用外壳 | 代码已实现 | 设置主页折叠大标题、共享卡组、Tab 与贴底/悬浮导航分支已落地；未设备验收 |
| 内容页、媒体页与长尾 | 共享层与重点页已实现 | 全局卡片/排版 Token 生效；首页、搜索、历史、收藏、空间与相关推荐封面已迁移；播放器生命周期未改；未设备验收 |

### UI-GAP-001：收口通用操作按钮

| 字段 | 内容 |
|---|---|
| 领域 / 优先级 / 状态 | 组件 / P2 / 待评估 |
| 维护角色 | 设计系统维护者、动态维护者、视频维护者 |
| 现状证据 | 源码存在至少三份 `ActionButton` 或相近实现，包括动态与视频详情领域；按压动画、图标容器和触摸区不同 |
| 目标 | 普通动作统一走 `AppButton`、`AppIconButton` 或一个共享语义入口；播放器/视频互动允许领域专用组件 |
| Why | 同名不同行为让初学者无法判断入口，也容易产生小于 48dp 的点击区 |
| 影响 | 动态卡、视频互动行、其他操作组 |
| 修改入口 | `AppPrimitiveComponents.kt`；各 feature 的 `ActionButton` |
| 依赖 / 风险 | 先分类语义；不能把“点赞计数操作”和“确认按钮”硬合并 |
| 实施步骤 | 列出调用点 → 按语义分组 → 确立共享入口 → 迁移普通场景 → 删除无调用旧实现 |
| 自动验证 | 结构测试、相关交互 policy 测试、48dp 规则测试 |
| 人工验证 | 双预设检查按下、选中、禁用、长文本与读屏名称 |

### UI-GAP-002：统一内容玻璃/卡片表面

| 字段 | 内容 |
|---|---|
| 领域 / 优先级 / 状态 | 卡片 / P2 / 待实施 |
| 维护角色 | 设计系统维护者 |
| 现状证据 | 搜索、消息、动态等存在至少三份 `GlassCard` 或同义外壳；已有 `ContentCardSurfacePolicy` 与 `AppCard` |
| 目标 | 一般卡面通过 `AppCard` + `ContentCardSurfacePolicy`，领域只提供内容和必要变体 |
| Why | 表面、边框、圆角和模糊降级应由主题统一 |
| 影响 | 列表性能、Miuix/Material/iOS 映射 |
| 修改入口 | `ContentCardSurfacePolicy`、`AppShapes`、各 feature `GlassCard` |
| 依赖 / 风险 | 动态转发卡、媒体封面等嵌套语义需保留；禁止卡中卡式装饰迁移 |
| 实施步骤 | 审计差异 → 定义变体 → 迁移通用卡 → 为领域例外写理由 |
| 自动验证 | `ContentCardSurfacePolicyTest` 与调用结构测试 |
| 人工验证 | 明/暗/AMOLED，关闭模糊，滚动列表不卡顿 |

### UI-GAP-003：统一错误与空状态

| 字段 | 内容 |
|---|---|
| 领域 / 优先级 / 状态 | 反馈 / P1 / 进行中（P005 试点完成） |
| 维护角色 | 设计系统维护者、各页面维护者 |
| 现状证据 | 旧 `LottieComponents.EmptyState/ErrorState` 与 feature 私有实现仍存在；P005 原先把分页失败写入整页 `error` |
| 目标 | 通过 `AppContentStateComponents.kt` 建立共享状态组件，明确标题、说明、主要恢复动作、次要动作与 PAGE/INLINE 变体 |
| Why | 错误必须可恢复，不能与空状态或未登录混淆 |
| 影响 | 全部网络页面与分页列表 |
| 修改入口 | `AppContentStateComponents.kt`、`SearchContentStatePolicy.kt`、`SearchViewModel.kt`、`SearchScreen.kt` |
| 依赖 / 风险 | 需保留页面已加载的可用数据；不能统一成整页遮挡 |
| 实施步骤 | 建立状态语义 → 设计共享 API → P005 试点 → 按阶段迁移消息和其他页面 |
| 自动验证 | `AppContentStatePolicyTest`、`SearchContentStatePolicyTest`、搜索 feature 测试、文档结构测试 |
| 人工验证 | 断网、重试、部分数据、分页失败、未登录逐项检查 |

**P005 试点证据（901a11954）**：已建立 `AppErrorState`/`AppEmptyState`，结果页按 body/footer policy 分离首次失败与分页失败；分页失败保留已有列表、页码和继续重试能力；热榜与搜索发现保留各自错误字段；系统返回和顶部返回先关闭建议/键盘。其余页面仍标记为待迁移，不能把本试点视为全局完成。

### UI-GAP-004：合并用户等级标签

| 字段 | 内容 |
|---|---|
| 领域 / 优先级 / 状态 | 身份 / P2 / 待实施 |
| 维护角色 | 设计系统维护者、个人空间维护者 |
| 现状证据 | 至少两份 `LevelTag`；项目已有共享 `UserLevelBadge` |
| 目标 | 一般用户等级只使用 `UserLevelBadge`，特殊直播/UP 身份采用不同语义名称 |
| Why | 等级颜色、文字和读屏说明应一致 |
| 影响 | 评论、个人空间、动态与视频作者信息 |
| 修改入口 | `UserLevelBadge` 及旧 `LevelTag` 调用点 |
| 依赖 / 风险 | 业务身份不能误当用户等级；需要检查等级 0 与未知值 |
| 实施步骤 | 分类标签 → 补齐共享变体 → 迁移 → 删除同义旧实现 |
| 自动验证 | 值域与组件入口测试 |
| 人工验证 | 长用户名、1.3 倍字体、明暗主题、读屏状态 |

### UI-GAP-005：PagesSelector 唯一入口

| 字段 | 内容 |
|---|---|
| 领域 / 优先级 / 状态 | 视频 / P2 / 待实施 |
| 维护角色 | 视频维护者 |
| 现状证据 | 至少两份 `PagesSelector`，职责和包位置不清晰 |
| 目标 | `video/ui/components` 下保留一个目标实现，其他调用迁移或改为明确不同语义名 |
| Why | 分 P/选集是同一业务动作，状态和焦点不能各自实现 |
| 影响 | 视频详情、播放器与宽屏详情 |
| 修改入口 | 两份 `PagesSelector` 文件及调用者 |
| 依赖 / 风险 | 选 P 与番剧选集可能不是同一数据合同，需先区分 |
| 实施步骤 | 对比 API → 确定视频目标入口 → 迁移 → 删除或重命名领域差异 |
| 自动验证 | 选择索引、禁用与滚动定位测试 |
| 人工验证 | 单 P、多 P、长标题、旋转、全屏进入与返回 |

### UI-GAP-006：补齐布局 Token 与断点所有权

| 字段 | 内容 |
|---|---|
| 领域 / 优先级 / 状态 | 布局 / P2 / 待评估 |
| 维护角色 | 设计系统维护者、页面维护者 |
| 现状证据 | 已有 0/2/4/8/12/16/24/32/48dp 间距；设置约 840dp、搜索 600/840dp 等页面特例分散；视频无独立媒体断点 |
| 目标 | 共享宽度等级由公共策略表达；页面特例有所有者、原因和回退；补充经验证的头像/媒体/最大宽度 Token |
| Why | 避免新页面复制偶然断点或继续新增近似尺寸 |
| 影响 | 手机、平板、分屏与折叠屏 |
| 修改入口 | layout policy、`DeviceUiProfile`、`AppSpacingTokens` |
| 依赖 / 风险 | 不能一次性替换媒体特例；需用真实布局语义验证 |
| 实施步骤 | 建断点清单 → 聚类共享语义 → 保留带注释特例 → 增加策略测试 |
| 自动验证 | 各断点 policy 单元测试 |
| 人工验证 | 360/700/840/960/1000/1600dp 切换并检查状态保留 |

### UI-GAP-008：双预设验收证据

| 字段 | 内容 |
|---|---|
| 领域 / 优先级 / 状态 | QA / P1 / 待实施 |
| 维护角色 | QA 维护者、设计系统维护者 |
| 现状证据 | Miuix 已深度接入，iOS/Material 3 没有与本手册对应的统一关键流程记录 |
| 目标 | 按 `09_ACCEPTANCE.md` 形成文字验收记录，Miuix 全矩阵，兼容风格关键矩阵 |
| Why | 能编译不代表状态、布局和返回行为一致 |
| 影响 | 所有用户可选主题 |
| 修改入口 | QA 记录与后续缺陷，不在本任务修改 UI |
| 依赖 / 风险 | 需要用户真实设备操作；不得使用截图作为规范输入 |
| 实施步骤 | 准备测试数据 → 执行 Miuix → 执行兼容流程 → 登记失败任务 |
| 自动验证 | 先运行结构和相关 policy 测试 |
| 人工验证 | 按主题/宽度/状态逐项填写文字结果 |

## Compose 短示例

```kotlin
// 目标：业务页调用共享语义入口，而不是复制一份 ErrorState。
AppErrorState(
    message = message,
    onRetry = onRetry
)
```

`AppErrorState` 在当前代码中若尚不存在，只表示目标 API 示例，实施前必须由任务卡确认命名和所有者。

## 代码映射

- 现状审计工具：`scripts/ui_architecture_audit.ps1`
- 共享卡面：`ContentCardSurfacePolicy`
- 共享身份：`UserLevelBadge`
- Token：`AppSpacingTokens`、`AppShapes`、`AppMotionTokens`

## 当前差距

本页列出首批已知差距，不代表完整缺陷清单。每次页面档案或人工验收发现新问题，应新增任务卡而不是修改目标规范去迁就现状。

## 验收方法

任务只有在自动验证通过、人工步骤有文字结果、目标代码入口唯一且旧入口已处理后才能标记完成；接受例外必须写期限或永久理由。
